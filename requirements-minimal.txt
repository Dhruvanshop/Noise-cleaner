# Minimal requirements for Render free tier
# Using versions with pre-built wheels (no compilation needed)

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Audio processing (lightweight, pre-built wheels)
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.12.0

# File handling
aiofiles>=23.2.1

# Core dependencies
starlette>=0.36.0
pydantic>=2.6.0

# Total size: ~150MB
