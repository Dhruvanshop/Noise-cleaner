# Absolute minimal for Render free tier
# NO scipy (needs Fortran compiler)
# Only pre-built wheels

# Core web framework
fastapi==0.109.2
uvicorn==0.27.1
python-multipart==0.0.9
aiofiles==23.2.1
pydantic==2.6.3
starlette==0.36.3

# Basic audio - only soundfile (has pre-built wheels)
numpy==1.26.4
soundfile==0.12.1

# Total: ~80MB, all pre-built wheels

